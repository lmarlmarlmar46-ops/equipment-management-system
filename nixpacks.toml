[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "cd backend && npm install --omit=dev"
]

[phases.build]
cmds = [
  "echo 'Backend build complete'"
]

[start]
cmd = "cd backend && node server.js"
